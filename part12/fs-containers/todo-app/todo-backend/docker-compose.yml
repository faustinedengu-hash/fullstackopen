services:
  app:
    image: todo-backend
    build: .
    ports:
      - 3000:3000
    container_name: todo-backend-container
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    # THE SLEDGEHAMMER: Force install before running dev
    command: sh -c "npm install && npm run dev"